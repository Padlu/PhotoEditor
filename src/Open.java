/*import javafx.application.Application;
import java.awt.Desktop;
import java.awt.image.ImageObserver;
import java.awt.image.ImageProducer;
import java.io.*;//for File, IOExcption
import java.net.URL;
import java.util.logging.Level;
import java.util.logging.Logger;
import javafx.event.*;//for Action Event, EventHandler
import javafx.geometry.Insets;
import javafx.scene.*;
import javafx.scene.image.ImageView;
import javafx.stage.Modality;
import javafx.stage.*;//contains Stage FileChooser

import java.awt.*;*/

/**
 * Created by Abhishek on 19-09-2016.
 */
/*public class Open extends Application {


    public static void display() {
        Stage window = new Stage();
        Desktop desktop = Desktop.getDesktop();
        ImageView imageView;

        final FileChooser fileChooser = new FileChooser();
        fileChooser.getExtensionFilters().add(new FileChooser.ExtensionFilter("Image Files", "*.png", "*.jpg", "*.gif"));

            File file = fileChooser.showOpenDialog(window);

            if (file != null) {

                    Image image = new Image() {
                        @Override
                        public int getWidth(ImageObserver observer) {
                            return 0;
                        }

                        @Override
                        public int getHeight(ImageObserver observer) {
                            return 0;
                        }

                        @Override
                        public ImageProducer getSource() {
                            return null;
                        }

                        @Override
                        public Graphics getGraphics() {
                            return null;
                        }

                        @Override
                        public Object getProperty(String name, ImageObserver observer) {
                            return null;
                        }
                    };
                    imageView.setImage(image);



            }

            }


    @Override
    public void start(Stage primaryStage) throws Exception {

    }
}*/
/*desktop.open(file)
 catch (IOException ex) {
         Logger.getLogger(
         Open.class.getName()).log(
        Level.SEVERE, null, ex
        );*/
